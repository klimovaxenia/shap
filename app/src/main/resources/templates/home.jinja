<html>
<body>
    <h1>Hello {{ name }}</h1>

    <h1>Stuff list</h1>

    <ul>
    {% for item in items %}
        <li>{{ item.name }} ({{ item.id }})</li>
    {% endfor %}
    </ul>

</body>
</html>
